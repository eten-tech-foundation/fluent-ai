# Git
.git
.gitignore

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
.venv
venv

# IDE
.idea
.vscode
*.swp
*.swo

# Testing
.pytest_cache
.coverage
htmlcov
src/tests
tests

# Build
*.egg-info
dist
build

# Local env files
.env
.env.dev
.env.prod
.db-initialized

# Docker
docker-compose*.yml
compose.yaml
.dockerignore
Dockerfile
Dockerfile.dev

# Scripts and orchestration helpers (not needed in the prod image).
# NOTE: `scripts/` is intentionally NOT excluded — CI/CD uses this same image
# to run `scripts/bootstrap.py` for out-of-band schema/role provisioning.
fai.sh
fai.ps1
fai
fai.bat
docker-entrypoint.sh

# Documentation and notes — not shipped to production
docs
*.md
AGENTS.md

# CI / tooling
.github
.gitlab
.idea
.vscode
